const app = require("./app");
require('dotenv').config()
    // middleware
    // routers




//port
const PORT = 5000




//server

app.listen(PORT, () => {
    console.log(`server is running on port ${PORT}`)
})